spring:
  application:
    name: cortex-spring-boot-starter-example
  ai:
    mcp:
      server:
        protocol: stateless
        type: sync
        instructions: Use this MCP for knowledge graph operations

cortex:
  ingest:
    persistent: true
